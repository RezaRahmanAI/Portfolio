# Project Images

Add project showcase images for the portfolio cards in this folder. Save each image with the following suggested filenames:

- tricon-properties.png
- prime-housing.png
- moto-power.png

The Ashaven Developers project currently references a remote image URL, so no local asset is required here.

Images in this directory are served statically and referenced by the entries in `src/data/projects.ts`.
